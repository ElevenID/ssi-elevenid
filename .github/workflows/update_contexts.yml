name: update_contexts

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
    - name: Checkout SSI library
      uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v6

    - name: Update context files
      run: crates/contexts/update.sh

    - name: Test embedded contexts
      run: cargo test --package ssi-contexts

    - name: Create pull request
      uses: peter-evans/create-pull-request@5f6978faf089d4d20b00c7766989d076bb2fc7f1 # v8
      with:
        commit-message: Update context files
        branch: fix/update-contexts
        title: Update context files
        labels: github_actions
        body: 'Auto-generated after HTTP, JSON-LD, and crate validation: https://github.com/ElevenID/ssi-elevenid/actions/runs/${{ github.run_id }}'
        author: ssi <noreply@didkit.dev>
